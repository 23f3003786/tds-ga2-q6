name: My First Action

on: [push]  # Triggers on any code push

jobs:
  test:
    runs-on: ubuntu-latest  # Free cloud computer (runner)
    steps:
      - name: Checkout code
        uses: actions/checkout@v4  # Grabs your repo files

      - name: 23f3003786@ds.study.iitm.ac.in  # Your required step name
        run: echo "Hello from my action!"

      - name: List files
        run: ls -la  # Shows what the robot sees
